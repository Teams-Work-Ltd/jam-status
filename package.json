{
  "name": "jam-status",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "check": "node --test && node scripts/validate-site.mjs",
    "serve": "node scripts/serve.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
